doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        section.plan
            .wrapper
                .main#plan
                    h3 MIsocial
                    h1 Select a Plan
                    p Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut.
            
                .flex
                    +card(cards.free)
                    +card(cards.basic)
                    +card(cards.vip)
        section.map.flex
            .wrapper
                p SOCAL MARKETING & ANALYTICS
                h3 Take control of your data
                .flex 
                    .btn
                        a(href="#" class="vio") GET STARTED
                    .btn
                        a(href="#" class="vio") EXPLORE FEATURES